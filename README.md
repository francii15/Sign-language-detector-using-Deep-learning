# Sign-language-detector-using-Deep-learning
Developed a 41-class Sign Language Recognition system using MobileNetV2 transfer learning with ImageNet weights. The project uses 18,450 images, data augmentation, two-stage training with fine-tuning, and a 41-class softmax classifier. The trained model supports single-image prediction and real-time webcam inference using OpenCV .
